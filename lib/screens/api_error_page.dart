import 'package:flutter/material.dart';

class ApiErrorPage extends StatelessWidget {
  const ApiErrorPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Api Error"),),
      
    );
  }
}